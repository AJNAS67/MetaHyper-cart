<%- include('./partials/userHeader.ejs') %>

<div class="container mt-3">
  <div class="main-body m-4">
    <div class="row">
      <div style="margin: auto" class="col-md-6 col-sm-12">
        <div class="card p-4" style="border-radius: 15px">
          <div class="card-body">
            <form action="/forgoet-password-otp" method="post" id="myForm">
              <p>
                We will send an OTP to your registered mobile number. Confirm
                the OTP to change the password
              </p>

              <% if (usernotfindError!= '') { %>
              <div class="alert alert-danger mt-3" role="alert">
                <p class="text-danger"><%=usernotfindError %></p>
              </div>
              <% } %>
              <div class="form-outline mb-4">
                <label class="form-label">Email address</label>
                <input
                  type="email"
                  class="form-control form-control-lg"
                  name="email"
                  id="email"
                  required
                />
              </div>

              <button
                id="loginButton"
                class="btn btn-dark btn-lg"
                type="submit"
              >
                SUBMIT
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include('./partials/userFooter.ejs') %>
